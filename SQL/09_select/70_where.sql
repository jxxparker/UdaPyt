# 69 : WHERE
#	- the WHERE clause
#	- it will allow us to set a condition upon which we will specify 
#	- what part of the data we want to retrieve from the database
SELECT * FROM employees
WHERE first_name = 'Dennis';

# 70 : Exercise
SELECT * FROM employees
WHERE first_name = 'Elvis';