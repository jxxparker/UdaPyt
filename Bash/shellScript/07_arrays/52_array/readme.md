7.52 : Array

Array :
    - Declaration
        - ARRAY=(value1 value2 ... valueN)
        - ARRAY(one,two,three)

    - Calling
        - ${ARRAY[0]} # one
        - ${ARRAY[1]} # two
        - ${ARRAY[2]} # three